{
  "name": "oms:plan",
  "command": "You are now in OMS planning mode — a strategic planning workflow that analyzes the codebase, runs a Planner→Architect→Critic consensus loop, produces a RALPLAN-DR + ADR decision artifact, and gates execution behind explicit user approval.\n\n## Goal\n\n$ARGUMENTS\n\n## Instructions\n\nFollow the OMS Plan skill instructions exactly. Load them by calling the `skill-execute` tool with `{ \"skill\": \"oms/plan\" }` and follow the SKILL.md procedure:\n\n1. **Start the session**: Call `oms-start` with the goal above and an optional verifyCommand (e.g. \"npm test\"). The session enters the `planning` stage — file edits are hard-blocked.\n2. **Classify the request**: Broad/vague → Interview mode; detailed → Direct mode; `--consensus` or \"ralplan\" → Consensus mode; `--review` → Review mode. Default to Interview for broad requests.\n3. **Gather codebase facts FIRST**: Delegate to `#oms_researcher` (external docs + code analysis) or `#oms_architect` (codebase structure) to find existing implementation. Never ask the user what you can look up yourself.\n4. **Consensus loop** (Consensus mode): Planner creates initial plan + RALPLAN-DR summary → `#oms_architect` reviews → `#oms_critic` evaluates → revise on rejection (max 5 iterations). Run Architect and Critic SEQUENTIALLY — never in parallel.\n5. **Produce decision artifact**: Generate RALPLAN-DR summary (Principles / Decision Drivers / Viable Options) + ADR (Decision / Drivers / Alternatives / Why chosen / Consequences / Follow-ups). Add `--deliberate` for high-risk work (auth/security, migrations, destructive changes, production incidents, compliance/PII, public API breakage) — adds pre-mortem + expanded test plan.\n6. **Approval gate** (CRITICAL): Present the plan via `askuser-ask_question` with options: Approve via `/oms:team` (parallel, recommended) / Approve via `/oms:ralph` (serial) / Approve via `/oms:auto` (autonomous) / Request changes / Reject. Non-interactive mode outputs `pending approval` and stops — do NOT auto-transition.\n7. **On approval**: Call `oms-prd` with `{ \"action\": \"refine\", \"task\": \"<task>\", \"stories\": [{\"title\": \"...\", \"acceptanceCriteria\": [\"...\"], \"priority\": 1}] }` to persist decisions as verifiable stories → save the full plan (with RALPLAN-DR + ADR) to `.snow/oms-state/plans/<slug>.md` → call `oms-set-stage { stage: \"executing\" }` → hand off to the chosen execution skill.\n8. **Review mode** (`--review`): Read the existing plan from `.snow/oms-state/plans/` or PRD via `oms-prd` action `list`, delegate to `#oms_critic`, return APPROVED / REVISE / REJECT.\n\n## Rules\n\n- NEVER auto-transition to `executing` without explicit user approval via `askuser-ask_question`. The planning stage produces a `pending approval` artifact and stops — it does not auto-execute.\n- NEVER run `#oms_architect` and `#oms_critic` in parallel — the Critic must see the Architect's output first.\n- NEVER edit source files, commit, push, or open PRs during planning. The `before-tool-call` hook hard-blocks `filesystem-*`; commit/push/PR must be avoided by convention.\n- NEVER use generic acceptance criteria (\"implementation is complete\") — replace with specific, testable criteria before refining the PRD.\n- Ask ONE focused question at a time during interviews — never batch multiple questions.\n- 80%+ of claims must cite file:line; 90%+ of acceptance criteria must be testable. No vague terms without metrics (\"fast\" → \"p99 < 200ms\").\n- Save plan artifacts to `.snow/oms-state/plans/`, NOT `.snow/plan/` (that belongs to snow-cli's own planning documents).\n- If the user says \"just do it\" without naming an execution path, output the `pending approval` plan and require an explicit choice — do NOT auto-transition.\n\nBegin by calling `oms-start` with the goal.",
  "type": "prompt",
  "description": "战略规划——分析代码库、驱动 Planner→Architect→Critic 共识循环、产出 RALPLAN-DR + ADR 决策文档，用户审批后才执行。用法: /oms:plan [--direct|--consensus|--review] [--deliberate] <目标>",
  "argsHint": "[--direct|--consensus|--review] [--deliberate] [--interactive] <目标>"
}
